export function nameValidation(str: string):boolean{
    // if(str){
    //     console.log(str.value)
    // } else {
    //     return false;
    // }
    // console.log(str)
    // return false;
    // if (!str.value) {
    //     return false
    // } else {
    //     let cstr: string = str.value;
    //     if (cstr.length > 40 || cstr.length < 8) {
    //         return false;
    //     }
    //     let valid: RegExpMatchArray | null = cstr.match(/^[A-Z][A-Za-z]+$/);
    //     return !!valid;
    // }
    return false;

}